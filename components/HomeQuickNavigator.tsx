import { Text, View, TouchableOpacity } from 'react-native';

export const HomeQuickNavigator = () => {
  return (
    <View className="mx-3 mb-[30%] h-80">
      <TouchableOpacity>
        <Text>Me</Text>
      </TouchableOpacity>
    </View>
  );
};
